import { Fragment } from "react"

/**
 * should contain some data, mostly id of estate to know where to put it
 */
export const FailuresForm = () => {
    return <Fragment>
        Failure form
    </Fragment>
}